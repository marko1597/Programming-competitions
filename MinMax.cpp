#include <cstdio>
#include <algorithm>
#include <string.h>
using namespace std;

int main(){
	int n,niz[100000];
	scanf("%d",&n);
	memset(niz,0,sizeof niz);
	for(int x=0;x<m;x++)scanf("%d",niz[x]);
	sort(niz,niz+n);
	
}
